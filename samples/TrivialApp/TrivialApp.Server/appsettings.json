{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "NextjsStaticHosting": {
    "RootPath": "clientdist" // must match where the Next.js app outputs are published. See property `NextJsOutputPublishRelativePath` in the csproj.
  }
}
